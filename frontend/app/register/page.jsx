"use client";
import Link from "next/link"; import { useRouter } from "next/navigation"; import { useState } from "react"; import { api } from "../../lib/api"; import { useAuth } from "../../components/AuthProvider";
export default function RegisterPage() { const router = useRouter(); const { signIn } = useAuth(); const [form, setForm] = useState({ name: "", email: "", password: "" }); const [error, setError] = useState(""); const [loading, setLoading] = useState(false); const submit = async (e) => { e.preventDefault(); setError(""); if (form.name.trim().length < 2) return setError("Please enter your name."); if (form.password.length < 6) return setError("Password must be at least 6 characters."); setLoading(true); try { const data = await api("/auth/register", { method: "POST", body: JSON.stringify(form) }); signIn(data.token, data.user); router.replace("/"); } catch (err) { setError(err.message); } finally { setLoading(false); } }; return <main className="mx-auto flex min-h-[calc(100vh-5rem)] max-w-xl items-center px-4 py-12"><div className="w-full rounded-[28px] border border-emerald-100 bg-white p-8 shadow-xl shadow-emerald-100/50"><p className="text-sm font-medium uppercase tracking-[.2em] text-emerald-600">Register</p><h1 className="mt-2 text-3xl font-bold">Create account</h1><form className="mt-8 space-y-5" onSubmit={submit}>{[["name","Full name","Your name","text"],["email","Email address","you@example.com","email"],["password","Password","At least 6 characters","password"]].map(([field,label,placeholder,type]) => <label key={field} className="block text-sm font-medium">{label}<input type={type} required minLength={field === "password" ? 6 : undefined} value={form[field]} onChange={(e) => setForm({ ...form, [field]: e.target.value })} placeholder={placeholder} className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-4 py-3"/></label>)}{error && <p className="text-sm text-red-600">{error}</p>}<button disabled={loading} className="w-full rounded-xl bg-emerald-600 px-4 py-3 font-semibold text-white disabled:opacity-70">{loading ? "Creating account…" : "Create account"}</button></form><p className="mt-6 text-sm">Already have an account? <Link href="/login" className="font-semibold text-emerald-600">Sign in</Link></p></div></main>; }